[project]
name = "chatcon"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "akasha", email = "jakasha79@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "chainlit>=2.8.3",
    "openai-agents>=0.4.0",
]

[project.scripts]
chatcon = "chatcon:main"

[build-system]
requires = ["uv_build>=0.8.15,<0.9.0"]
build-backend = "uv_build"
